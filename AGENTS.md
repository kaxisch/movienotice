# MovieNotice 專案維護規則

本文件記錄專案擁有者明確指定、不可自行變更的產品與資料規則。修改程式、工作流程、文件或介面前，必須先確認沒有違反本文件。若需求與本文件衝突，必須先向專案擁有者說明影響並取得明確同意，不得自行推定可以改變既有規則。

## 語言與用語

- 中文內容一律使用繁體中文及台灣慣用語。
- 中文介面文字、文件、註解與提交訊息不得混用簡體字或中國慣用語。
- TMDB `zh-TW`、台灣影城及人工資料必須保留來源原文，不得在最終輸出階段對整份資料執行 OpenCC 或其他機械式簡繁轉換，以免把「干擾」等正確用字誤轉。
- 使用英文撰寫時不受上述中文用語限制。

## 公開網站的電影資料

- 公開網站只能顯示 TMDB `release_dates` 中具有台灣地區（`TW`）首映（type 1）、電影院／有限上映（type 2）或電影院（type 3）日期的電影。
- 同一部電影具有多筆符合條件的台灣日期時，公開資料只能保留最新日期；《劇場版 吉伊卡哇 人魚島的秘密》（TMDB 1586876）因中、日文版本相差一週，明確例外保留兩個語言版本日期。
- 不得僅因開眼電影網列出某部電影，就讓該電影出現在公開網站。
- 公開資料不得包含或公開開眼電影網的電影 ID、網址或其他僅供私人稽核使用的欄位。
- 不得移除或繞過 `scripts/validate_movie_data.py` 中針對 TMDB 台灣院線日期、資料來源與未來 180 天範圍的驗證；持續上映的現正熱映電影不受過去 180 天限制。
- 任何想要放寬公開條件、改用開眼日期直接上架或接受非 TMDB 台灣院線日期的修改，都必須先取得專案擁有者明確同意。

## 開眼稽核流程

- 開眼爬蟲只用於私人稽核、TMDB 配對及 Google Sheet 候選資料。
- `scripts/weekly_check.py` 必須維持僅執行稽核，不得恢復可直接重建或發布網站的 `--mode site` 等功能。
- 每週三、週六的開眼稽核可以更新日期工作表及 `_candidates`，但不得直接更新公開網站電影資料。
- `_candidates` 必須保留曾在開眼出現、且仍可能影響公開網站的電影狀態，包括連續缺席次數及最後出現日期；只有不同日期且完整成功的稽核才能累加缺席，同一日期重跑不得重複累加。
- 已因缺席隱藏的候選，只有在不再公開、不在白名單或人工保留清單，且 TMDB 台灣院線上映日期已超過 180 天後，才可從 `_candidates` 移除；180 天只作為私人候選清理門檻，不得再作為持續上映電影的公開下架期限。
- 開眼資料與 Google Sheet 稽核內容屬於私人工作資料，不得加入公開網站資源。

## 重映稽核流程

- 每週三、週六的私人稽核應以開眼、秀泰、國賓及威秀片單建立重映候選聯集，寫入固定的 `_rereleases` 工作表；不得直接發布網站。
- 威秀可提供「仍在上映」證據，但不得繞過 Akamai 或其他存取限制。威秀回傳 403 時應標示來源失敗，且不得把威秀列為缺席稽核的必要來源。
- 重映缺席稽核只有在開眼、秀泰、國賓及 TMDB 處理皆成功時才算完整；完整稽核中所有必要來源都未出現的候選，第一次缺席後立即隱藏。同一天重跑不得重複累加。
- 開眼仍列出但顯示多年舊日期的電影可作為私人重映待查訊號，但重新配對後仍須以同一部 TMDB 電影的台灣院線歷史重新確認，不得把另一部同名／改編作品的舊日期沿用到本片。
- 不得僅因 TMDB 全球原始上映日比台灣上映日早一年以上，就把台灣首次上映的電影判定為重映；自動確認重映必須有明確重映標記，或同一 TMDB 電影具有至少早一年、符合 type 1、2 或 3 的更早台灣院線日期。
- 開眼多年舊日期只能保存為私人稽核用的原標示日期，不得當成本次重映日期；缺少本次日期時應標示「上映日期待確認」。
- 重映片公開前，TMDB `release_dates` 必須具有與本次影城上映日期完全一致的 `TW` type 1、2 或 3 日期；不得沿用多年前的 TMDB 日期直接上架。
- 本次影城上映日期與 TMDB `TW` type 1、2 或 3 日期完全一致時，即使尚無法確認是否為重映，也必須以一般院線候選交給 Refresh，不得因重映證據不足而漏片；只有具備既有台灣上映紀錄、明確重映標示或人工確認的電影才能顯示「重映」標籤。
- 人工確認為重映只補足重映身分證據，仍不得省略本次影城上映日期與 TMDB 台灣院線日期完全一致的公開驗證。
- 歷史重映候選即使保留在 `_rereleases`，只有本輪 `rerelease_present` 仍為真、未隱藏且日期驗證成功時，才能覆蓋一般首輪候選或取得公開重映標記。
- 開眼本期首輪卡片即使缺少上映日期也不得整部略過；可保留存在訊號，再由 TMDB 台灣 type 1、2 或 3 日期補值及驗證。
- `_rereleases`、影城來源網址、開眼原標示日期及其他稽核欄位均屬私人資料，不得加入公開網站資源。

## TMDB Refresh 流程

- `scripts/refresh_site_from_google_sheet.py` 執行 Refresh 時不得重新爬取開眼電影網。
- Refresh 應讀取 Google Sheet `_candidates`、`_rereleases`、目前已發布電影、白名單及人工保留電影，再向 TMDB 重新確認台灣院線上映日期。
- Refresh 必須另外探索今天至距今第 180 天、具有 TMDB 台灣院線上映日期的電影，以涵蓋開眼尚未列出，或最近才在 TMDB 補登台灣上映日期的電影。
- 遠期探索只能把 TMDB Discover 結果當作候選 ID；公開前仍須逐部以 TMDB `release_dates` 確認 `TW` type 1、2 或 3 日期。
- TMDB 遠期探索電影只在距上映第 61～180 天期間免受院線缺席影響；進入上映前 60 天後，若尚無 `_candidates` 狀態則先停止公開，並由下一次完整成功的院線稽核建立或接手候選狀態。已有候選狀態時，稽核不完整不得使其隱藏。
- Refresh 在距上映 0～60 天內新探索到、且尚無 `_candidates` 狀態的電影，不得只依 TMDB 台灣院線日期公開；必須先由院線稽核建立候選狀態，或列入人工保留清單。任一院線來源仍列出時保留，所有必要來源皆缺席時隱藏；稽核不完整時不得把既有候選隱藏。
- 一般首輪片不分「現正熱映」或「即將上映」，在一次完整成功的院線稽核中若開眼、秀泰及國賓全部缺席，即應隱藏；威秀、光點華山及真善美若成功且仍列出該片，亦可作為繼續保留的存在證據，但其抓取失敗不得阻擋完整缺席判定。任一來源仍出現時，缺席次數歸零並維持顯示；已隱藏後重新出現者必須視為新一輪上映／重映候選。
- 已公開且每次完整成功的院線稽核仍由至少一個來源持續列出的現正熱映電影，不得因上映日期超過 180 天而下架；應保留同一輪上映狀態，直到一次完整稽核確認所有來源缺席。已達缺席門檻後再次出現者視為新一輪上映／重映候選，不得沿用多年前的 TMDB 日期直接恢復公開。
- 人工保留電影不受開眼缺席次數影響。由 TMDB 遠期探索取得、且仍距上映 61 天以上的電影不得因開眼缺席而隱藏。
- 開眼、秀泰、國賓或 TMDB 任一必要來源處理失敗，或開眼結果數量異常時不得累加缺席，避免因稽核故障造成公開電影誤下架。
- TMDB 連線重設、逾時、5xx 或其他暫時性讀取失敗時，Refresh 必須先重試；重試仍失敗時，須保留上一版已驗證台灣院線日期且仍在有效日期範圍內的電影，不得將暫時性失敗視為下架依據。
- 只有 TMDB 成功回應，且確認電影沒有符合公開條件的 `TW` type 1、2 或 3 日期時，才可從公開網站移除；持續上映電影可使用已驗證的原院線日期，不受過去 180 天限制。不得因暫時性 TMDB 讀取失敗放寬或跳過驗證。
- 目前 Refresh 為每三小時一次。專案擁有者已表示日後會另行改成每六小時一次；在收到明確指示前不得自行變更排程。

## PWA 與品牌名稱

- 手機「加入主畫面」的預設名稱固定為「電影佈告欄」。
- `manifest.webmanifest` 的 `name` 與 `short_name` 必須保持為「電影佈告欄」。
- `index.html` 的 `apple-mobile-web-app-title` 必須保持為「電影佈告欄」。
- 除非專案擁有者明確要求改名，否則不得以品牌統一、SEO 或其他理由修改上述三項名稱。

## Tab 切換與捲動位置

- 切換「現正熱映」與「即將上映」Tab 時，卡片開始顯示的位置不得產生跳動。
- 若切換前搜尋／篩選列已置頂，切換後必須維持置頂，不得讓 Logo 列重新出現。
- 若切換前頁面尚未往下捲、Logo 列仍位於頁面頂端，切換後必須維持相同位置，不得突然把搜尋／篩選列置頂。
- 修改 Tab、篩選、搜尋、Sticky 導覽列或捲動程式時，必須同時驗證以上兩種狀態。

## 修改與驗證原則

- 不得因重構、清理死程式碼、SEO、效能改善或流程簡化而暗中改變上述產品規則。
- 涉及資料來源、公開條件、保留條件、排程、PWA 名稱或 Tab 捲動行為的修改，完成前必須明確檢查本文件。
- 若無法確定修改是否會影響上述規則，應先停止相關修改並詢問專案擁有者。

## GitHub 發布流程

- 專案擁有者說「推上去」時，代表已明確授權完整發布流程：確認本次修改範圍、執行必要驗證、提交、推送、合併至 `main`，並確認正式部署結果；不得只停在功能分支或草稿 Pull Request。
- 若修改需要重新產生公開網站資料，完整發布流程也包含手動觸發必要的 TMDB Refresh、等待資料驗證與頁面重建完成，並確認後續正式部署成功。
- 完整發布時應儘量合併可安全一起執行的操作，避免要求專案擁有者反覆確認相同範圍的 GitHub 權限。
- 若驗證失敗、發現非本次修改的未提交內容、無法乾淨合併或部署失敗，必須停止自動合併並向專案擁有者說明，不得為了完成發布而略過檢查或混入無關修改。

### 「推上去」執行狀態

- 收到「推上去」後，必須立即進入完整發布狀態，不得在提交、推送、建立 Pull Request、合併或部署驗證之間再次以對話詢問是否繼續。
- 工具平台若強制顯示權限核准視窗，可以提出必要的工具核准；不得另外用文字重複詢問相同授權。
- 應優先合併可安全一起執行的 GitHub 操作，並使用已核准的命令前綴，減少權限核准視窗。
- 合併 Pull Request 不代表發布完成。只有在正式網址讀到本次版本或變更內容後，才可向專案擁有者回報完成。
- 若某種部署驗證方式失敗、被拒絕或中止，必須立即改用其他安全的唯讀方式驗證，不得停在「已合併」。
- 完整發布的必要檢查點固定為：
  1. 確認修改範圍。
  2. 執行必要驗證。
  3. 提交。
  4. 推送。
  5. 建立並合併 Pull Request 至 `main`。
  6. 確認部署檢查成功。
  7. 從正式網址讀回並確認本次變更。
- 未完成第 7 項前，不得使用「發布完成」、「已推上去」或其他表示完整發布已結束的措辭。
